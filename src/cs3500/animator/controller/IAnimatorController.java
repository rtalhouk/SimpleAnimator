package cs3500.animator.controller;

import cs3500.animator.view.IViewListener;

/**
 * Represents a controller interface. It has no methods becaue the inteface extends another that
 * contains all the features that has methods for all the actions that could come from the view.
 */
public interface IAnimatorController extends IViewListener {

}
